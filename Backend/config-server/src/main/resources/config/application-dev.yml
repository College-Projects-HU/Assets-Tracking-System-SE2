spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/assets_db
    username: dev_user
    password: dev_password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
logging:
  level:
    org.springframework: DEBUG
    com.assets: DEBUG
